const boyos = document.querySelectorAll("butto");

botoes.for Each = false;
botao.addEventListener("click", botaoClicado);
function botaoClicado(){
    let texto = botao.queySelector("span");
if (curtiu ===false) {
    texto.textContent++;
    curtiu= true;
}
}
    
